apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goldenapp.fullname" . }}-test-connection"
  labels:
    {{- include "goldenapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goldenapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
